<!--
<p id="notice"><%= notice %></p>

<p>
  <strong>Teacher:</strong>
  <%= @classroom.teacher_id %>
</p>

<p>
  <strong>School:</strong>
  <%= @classroom.school_id %>
</p>

<p>
  <strong>Grade:</strong>
  <%= @classroom.grade %>
</p>

<p>
  <strong>Content area:</strong>
  <%= @classroom.content_area %>
</p>

<p>
  <strong>Active:</strong>
  <%= @classroom.active %>
</p>

<%= link_to 'Edit', edit_classroom_path(@classroom) %> |
<%= link_to 'Back', classrooms_path %>
-->
<!-- Page Header-->
<header class="page-header">
  <div class="container-fluid">
    <h2 class="no-margin-bottom"><%= @teacher.name %>'s <%= @classroom.grade %> grade <%= @classroom.content_area %> class</h2>
  </div>
</header>

<section class="tables">   
  <div class="container-fluid">
    <div class="row">
      <div class="col-lg-12">
        <div class="card">
          <div class="card-header d-flex align-items-center">
            <h3 class="h4">Observations</h3>
          </div>
          <div class="card-body">
            <table class="table table-striped table-hover">
              <thead>
                <tr>
                  <th>Observation Date</th>
                </tr>
              </thead>
              <tbody>
                <% @observations.each do |observation| %>
                  <tr class='clickable-row' data-href='/observations/<%= observation.id %>'>
                    <th scope="row">1</th>
                    
                  </tr>
                <% end %>
              </tbody>

            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
<section></section>
