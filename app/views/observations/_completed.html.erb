<section class="client">
  <div class="container-fluid">
    <div class="row">
      <!-- Client Profile -->
      <div class="col-lg-12">
        <div class="card">
          <div class="card-body">
            <%= render 'results' %>
            <% if can? :manage, Observation %>
              <div class="row">
                <div class="col-md-2">
                  <%= link_to 'Edit', observation_observation_form_index_path(@observation), class:'btn btn-primary' %>
                </div>
                <div class="col-md-2">
                  <%= render partial: "partials/forms/modal-form", locals: {title: "Delete", form: "delete/delete-observation"} %>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
