<section class="client">
  <div class="container-fluid">
    <div class="row">
      <!-- Client Profile -->
      <div class="col-lg-12">
        <div class="card">
          <div class="card-body">
            <%= render 'results' %>
            <% if can? :manage, Observation %>
              <%= link_to 'Edit', observation_observation_form_index_path(@observation), class:'btn btn-primary' %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
