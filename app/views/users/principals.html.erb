<!-- Page Header-->
<header class="page-header">
  <div class="container-fluid">
    <h2 class="no-margin-bottom">Principals</h2>
  </div>
</header>
<!-- Principal List -->
<section class="tables">   
  <div class="container-fluid">
    <div class="row">
      <div class="col-lg-12">
        <div class="card">
          <% if @principals.empty? %>
            <div class="card-body">
              <%= render partial: "partials/texts/empty", locals: {missing: "Principals"} %>
            </div>
          <% else %>
            <div class="card-header d-flex align-items-center">
              <h3 class="h4">All Principals</h3>
            </div>
            <div class="card-body">
              <table class="table table-striped table-hover">
                <thead>
                  <tr>
                    <th>#</th>
                    <th>Name</th>
                    <th>Grade</th>
                    <th>Content Area(s)</th>
                  </tr>
                </thead>
                <tfoot>
                  <tr>
                    <td>
                      <a href="#">View All</a>
                    </td>
                  </tr>
                </tfoot>
                <tbody>
                  <% @principals.each do |principal| %>
                    <tr class='clickable-row' data-href='/users/<%= principal.username %>'>
                      <th scope="row">1</th>
                      <td><%= principal.name %></td>
                      <td><%= principal.highestRole %></td>
                      <td><%= principal.username %></td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</section>