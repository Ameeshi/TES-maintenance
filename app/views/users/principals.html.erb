<!-- Page Header-->
<header class="page-header">
  <div class="container-fluid">
    <h2 class="no-margin-bottom">Principals</h2>
  </div>
</header>
<%= render "partials/alerts" %>
<!-- Principal List -->
<section class="tables">   
  <div class="container-fluid">
    <div class="row">
      <div class="col-lg-12">
        <div class="card">
          <% if @principals.empty? %>
            <div class="card-body">
              <%= render partial: "partials/texts/empty", locals: {missing: "Principals"} %>
            </div>
          <% else %>
            <div class="card-header d-flex align-items-center">
              <h3 class="h4">All Principals</h3>
            </div>
            <div class="card-body">
              <table class="table table-striped table-hover">
                <thead>
                  <tr>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Username</th>
                  </tr>
                </thead>
                <tfoot>
                  <tr>
                    <td colspan="100%">
                      <div class="apple_pagination">
                        <%= will_paginate @principals, :container => false %>
                      </div>
                    </td>
                  </tr>
                </tfoot>
                <tbody>
                  <% @principals.each do |principal| %>
                    <tr class='clickable-row' data-href='/users/<%= principal.username %>'>
                      <td><%= principal.list_name %></td>
                      <td><%= principal.email %></td>
                      <td><%= principal.username %></td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</section>